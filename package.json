{
  "name": "whale-watching-booking-engine",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "backend",
    "frontend",
    "operator"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "preview": "npm run preview -w frontend",
    "dev:operator": "npm run dev -w operator",
    "build:operator": "npm run build -w operator",
    "preview:operator": "npm run preview -w operator"
  }
}
